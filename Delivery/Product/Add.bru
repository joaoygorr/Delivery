meta {
  name: Add
  type: http
  seq: 2
}

post {
  url: {{URL}}/product
  body: json
  auth: inherit
}

headers {
  Authorization: {{TOKEN}}
}

body:json {
  {
    "name": "teste"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
