meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: {{URL}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "admin@gmail.com",
    "password": "admin123"
  }
}

settings {
  encodeUrl: true
}
